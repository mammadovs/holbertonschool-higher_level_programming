Tests for BaseGeometry class
============================

Importing the class:
    >>> BaseGeometry = __import__('7-base_geometry').BaseGeometry
    >>> bg = BaseGeometry()

Testing area() method:
    >>> try:
    ...     bg.area()
    ... except Exception as e:
    ...     print(e)
    area() is not implemented

Testing integer_validator with valid integers:
    >>> bg.integer_validator("my_int", 10)
    >>> bg.integer_validator("width", 89)

Testing integer_validator with non-integer values:
    >>> try:
    ...     bg.integer_validator("height", "10")
    ... except TypeError as e:
    ...     print(e)
    height must be an integer

    >>> try:
    ...     bg.integer_validator("width", 10.5)
    ... except TypeError as e:
    ...     print(e)
    width must be an integer

Testing integer_validator with values <= 0:
    >>> try:
    ...     bg.integer_validator("depth", 0)
    ... except ValueError as e:
    ...     print(e)
    depth must be greater than 0

    >>> try:
    ...     bg.integer_validator("distance", -5)
    ... except ValueError as e:
    ...     print(e)
    distance must be greater than 0

Checking for required documentation:
    >>> module_doc = __import__('7-base_geometry').__doc__
    >>> len(module_doc) > 20
    True
    >>> class_doc = BaseGeometry.__doc__
    >>> len(class_doc) > 20
    True
    >>> method_doc = BaseGeometry.integer_validator.__doc__
    >>> len(method_doc) > 20
    True
